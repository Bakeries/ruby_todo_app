<%= form_with(model: [@todo_item.todo_list, @todo_item]) do |f| %>
  <% if @todo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo_item.errors.count, "error") %> prohibited this todo_item from being saved:</h2>

      <ul>
      <% @todo_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-group flex-nowrap">
    <span class="input-group-text" id="addon-wrapping">Description</span>
    <%= f.text_field :description, class: 'form-control', placeholder: 'List title'%>
    <%= f.submit "Update", class: 'btn btn-warning input-group-btn' %>
  </div>
<% end %>
